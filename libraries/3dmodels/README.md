# Custom 3D models

Place repository-owned KiCad STEP or WRL models here and reference them with `${KIPRJMOD}/libraries/3dmodels/...` paths.

No custom 3D model files were present when this directory was created.
